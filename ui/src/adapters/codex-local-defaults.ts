// readonly-core: inlined defaults from @paperclipai/adapter-codex-local.
// The package is removed from this build (local agent engines are disabled);
// these values are kept so existing codex_local agent configs remain readable.
export const DEFAULT_CODEX_LOCAL_MODEL = "gpt-5.3-codex";
export const DEFAULT_CODEX_LOCAL_BYPASS_APPROVALS_AND_SANDBOX = true;
